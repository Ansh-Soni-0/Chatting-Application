for good icon use lucide-react
for good components use daisyUI

in zustand if we want to use any state or any funtion inside any other funtion then use get which is provided in the zustand

example:

export const useAuthStore = create((set , get) => ({
    
login:  () => {   
    get().connectSocket()    
},
connectSocket : () => {}

)}



way to get socket state from other zustand file
const socket = useAuthStore.getState().socket;